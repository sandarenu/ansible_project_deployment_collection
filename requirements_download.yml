
# This requirements file is used package the ansible collections so they can be copied to
# production servers
#
collections:
# With the collection name, version, and source options
- name: sansoft.common_tasks
  type: git
  source: 'https://github.com/sandarenu/ansible_common_tasks_collection.git#/sansoft/common_tasks'
- name: sansoft.project_1_tasks
  type: dir
  source: './ansible_collections/sansoft/project_1_tasks'
